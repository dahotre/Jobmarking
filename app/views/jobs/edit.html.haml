%legend Hope you find this job useful

= render :partial => 'jobs/job', :locals => { :j => @job, :full => true, :owner => true, :show_actions => false }
= render 'form'

= link_to 'Show', @job
\|
= link_to 'Back', jobs_path
